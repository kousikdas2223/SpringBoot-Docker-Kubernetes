server:
  port: 8090

spring:
  application:
    name: "loans"
  profiles:
    active: prod
  datasource:
    url: jdbc:mysql://localhost:3310/loansdb
    username: root
    password: root
  sql:
    init:
      mode: always
  jpa:
    show-sql: true
  config:
    import: "optional:configserver:http://localhost:8071"

management:
  metrics:
    enable:
      system: false
  endpoints:
    web:
      exposure:
        include: "*"
